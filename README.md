# chintechuk.github.io

Static GitHub Pages site for `chintechuk.org`.

The current site is an English inquiry-led catalogue for selected heritage-inspired artworks. It uses a static `index.html`, `styles.css`, and the preserved `CNAME` file for the custom domain.
